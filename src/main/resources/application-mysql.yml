spring:
  datasource:
    url: jdbc:mysql://localhost:15000/movie
    username: root
    password: your_password
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true

server:
  port: 18000